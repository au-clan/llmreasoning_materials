"""All live demo material for the tutorial, in one place.

Sub-packages:
  * ``reasoning_demo`` — the test-time reasoning-strategies engine (tutorial §2.1).
  * ``instability``    — the consistency/instability eval views (tutorial §1).

``serve.py`` is the single launcher for every browser demo
(``uv run python demo/serve.py``); ``../run_demo.py`` is the Part-2 CLI.
"""
